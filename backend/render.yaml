services:
  - type: web
    name: github-dashboard-backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GITHUB_TOKEN
        sync: false
      - key: PORT
        value: 5000
